<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.jinva02.top10downloader.MainActivity"
    tools:showIn="@layout/activity_main">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/parseBtn"
        android:text="Parse XML File"
        android:layout_marginLeft="20dp"/>

    <ListView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/xmlListView"
        android:layout_below="@+id/parseBtn"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="@+id/parseBtn"
        android:layout_marginTop="10dp"/>

</RelativeLayout>
